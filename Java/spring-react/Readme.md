# Basic Spring Boot REST + React TS

## Local development

```bash
cd frontend
npm i
cd ..
mvn spring-boot:run
# open http://localhost:8080

cd frontend
npm start
# open http://localhost:3000
```

## Prod build

```bash
mvn clean install
```
